services:
  minecraft-village-bot:
    build: .
    container_name: minecraft-village-bot
    restart: unless-stopped
    environment:
      - MC_HOST=46.224.3.29
      - MC_PORT=25565
      - MC_USERNAME=cr4zy_chicken
      - MC_VERSION=1.20.1
    volumes:
      - ./data:/app/data
      - ./schematics:/app/schematics:ro
      - ./.env:/app/.env:ro  # ← .env mounten!
    stdin_open: true
    tty: true
